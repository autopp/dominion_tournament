---
applications:
  - name: dominion-tournament
    buildpack: https://github.com/cloudfoundry/ruby-buildpack.git
    command: bundle exec rake db:migrate && bundle exec rails server -b 0.0.0.0
    env:
      RAILS_ENV: production
      TZ: Asia/Tokyo
